name: Validate GitHub Work Mode
on:
  push:
    branches: [feature/github-work-mode]
    paths:
      - 'app/**'
      - 'scripts/github-work-mode.b64.*'
      - '.github/workflows/validate-github-work-mode.yml'
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: recursive
      - name: Prepare GitHub Work Mode source
        id: patch
        continue-on-error: true
        run: |
          if grep -q 'data class GitHubWorkSnapshot' app/src/main/java/com/jarves/mh/ui/GitHubConnectSupport.kt; then
            echo 'GitHub Work Mode implementation already present' > github-work-mode-validation.txt
          else
            cat scripts/github-work-mode.b64.* | base64 -d | gzip -d > /tmp/add_github_work_mode.py
            python3 /tmp/add_github_work_mode.py > github-work-mode-validation.txt 2>&1
          fi
          git diff --check >> github-work-mode-validation.txt 2>&1
      - uses: actions/setup-java@v5
        if: steps.patch.outcome == 'success'
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - name: Configure Android SDK
        if: steps.patch.outcome == 'success'
        run: |
          SDKMANAGER="$(find "$ANDROID_SDK_ROOT/cmdline-tools" -type f -path '*/bin/sdkmanager' | sort -V | tail -n 1)"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platforms;android-36" "build-tools;36.0.0" "build-tools;35.0.0" "ndk;26.1.10909125" "cmake;3.22.1"
      - name: Compile, test, lint and assemble
        if: steps.patch.outcome == 'success'
        id: gradle
        continue-on-error: true
        run: |
          chmod +x gradlew
          ./gradlew clean compileOnlineReleaseKotlin testOnlineReleaseUnitTest lintOnlineRelease assembleOnlineDebug --stacktrace >> github-work-mode-validation.txt 2>&1
      - name: Commit diagnostics
        if: always()
        run: |
          echo "PATCH=${{ steps.patch.outcome }}" >> github-work-mode-validation.txt
          echo "VALIDATION=${{ steps.gradle.outcome }}" >> github-work-mode-validation.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add github-work-mode-validation.txt
          if ! git diff --cached --quiet; then
            git commit -m "Validate GitHub Work Mode"
            git push
          fi
          test "${{ steps.patch.outcome }}" = "success"
          test "${{ steps.gradle.outcome }}" = "success"
